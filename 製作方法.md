# 英文文法學習網 製作方法

> 線上版：<https://anitatsai13.github.io/english-grammar-site/>
> 單一自包含 HTML＋PWA（manifest / sw.js `grammar-v1` / icon.svg），無框架、無 build step。

## 檔案結構

```
english-grammar-site/
├── index.html            ← App 本體（學習卡＋題庫＋測驗＋錯題本）
├── manifest.webmanifest
├── sw.js                 ← 快取名 grammar-v1，改版要升版號
└── icon.svg
```

## 內容架構：冊 → Unit → 主題卡

- **HTML 卡片**：每個文法主題一張 `<section class="gcard" data-topic="主題名">`，內含句型表（`.twrap>table.gt`）、要點（`ul.pt`）、觀念不NG（`.ng`）、訣竅（`.tipbox`）、展開圖（`.xmap`）與「📝 練習此主題」按鈕。
- **冊別分頁**：卡片包在 `<div class="lpane" id="learn-b1…b7">`；Unit 用 `.uhead`（單元標頭）＋`.utodo`（卡片待補清單）分隔。
- **BOOKS 常數**是目錄中樞：`{bk:{name,topics:[…],units:[{u,sub,topics,todo}]}}`——新卡要三處同步：lpane 插 section、utodo 移除該項、BOOKS 的 topics 與 units[i].topics 加入。
- 主題名不可跨冊重複（例：b4 已有「對等連接詞」，b7 用「對等連接詞 not only／either／neither」）。

## 題庫

- `const QS=[{topic,src,rate,q,opts,ans,exp,tip}]`（800+ 題，單行 JSON）。
- `topic` 必須對得上某冊 BOOKS.topics（`bookOf()` 反查歸冊）；`src` 慣例：文法基礎題／觀念統整題／必考基礎題／必練進階題／複習講義／「1XX 會考」（rate＝通過率）。
- **收題規則**：附圖題一律不收；新增會考題前務必用題幹文字查重（同題可能掛在不同主題下）。

## 測驗與錯題本

- 「練習此主題」→ 設定 sel → 切到測驗分頁；`bookQS()` 依當前冊別＋主題篩題。
- 作答回饋框 `.fb`（20px 粗體）顯示 ✅/❌＋解析＋📌 訣竅；錯題記 localStorage，錯題本支援冊別／主題篩選重測。
- **migrateFromVocab**：登入時從 Supabase `grammar_inbox` 把單字網送來的文法錯題轉檔併入（反查原題、round|t|qk|topic 去重，重複執行安全）。

## 搜尋

- `S_CARDS`＝載入時掃描全部 `.gcard` 建索引（標題＋內文小寫），搜尋結果分「學習重點（點擊前往）」與題目兩區。

## 維護流程

1. 改 `index.html` → `new Function()` 驗證所有 `<script>` 語法 → 本機 node http server＋瀏覽器實測（分頁、練習、回饋）
2. **改前必先 `git fetch` 比對**（此 repo 常在其他環境被更新，直接 push 會被拒）
3. commit（中文訊息＋AI 協助範圍）→ push → GitHub Pages 自動部署 → 內容標記輪詢確認上線
